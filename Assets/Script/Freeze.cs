using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class Freeze : MonoBehaviour {

    //private HitStatus isHit;
    //private PowerUp isPowerUP;

    //Collider m_Collider;
    //NavMeshAgent movement;
    //Renderer mesh;
    //Color originalColor;
    //float originalSpeed;

    //private void Start()
    //{
    //    isPowerUP = GameObject.Find("Relic(Clone)").GetComponent<PowerUp>();
    //    isHit = this.GetComponentInChildren<HitStatus>();
    //    m_Collider = this.GetComponent<Collider>();
    //    movement = this.GetComponent<NavMeshAgent>();
    //    mesh = this.GetComponentInChildren<Renderer>();

    //    originalColor = mesh.material.color;
    //    originalSpeed = movement.speed;
    //}

    //private void Update()
    //{
    //    if (isHit.hit && isPowerUP.powerStatus)
    //    {
    //        m_Collider.enabled = false;
    //        movement.speed = 0;
    //        mesh.material.color = Color.white;
    //    }
    //}
}
